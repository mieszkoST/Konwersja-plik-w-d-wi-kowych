void * memset ( void * buffer, int c, size_t num );

Jest to funkcja biblioteczna z plikiem naglowkowym stirng.h. Dziala ona na zasadzie wypelniania kolejnych bajtów w pamięci
ustaloną wartością. Jej argumenty to : 
1) buffer - adres początkowy
2) c - wpisywana wartość (dla napisu - numer znaku)
3) num - ile bajtów zapisać.
